# Personal learning notes

## Jargons
- Q values: the expected reward at particular state S and taking action a. They are also called `action values`.
- Usually value based method takes a state, and updates the q table (which contains q values). The actions is then sampled from the table or randomly picked based on the given epsilon value (and decay)
