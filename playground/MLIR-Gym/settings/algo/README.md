# This file illustrate some fields in the config
- ppo_config
    - env.rounds: How many steps to apply per episode
    - ppo.n_steps: How many episodes to run before PPO update (gradient descent)
    - ppo.timesteps: How many updates to run

